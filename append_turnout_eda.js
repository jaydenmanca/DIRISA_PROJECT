const fs = require('fs');
const path = 'model.ipynb';
const notebook = JSON.parse(fs.readFileSync(path, 'utf8'));
const add = (cell_type, source) => notebook.cells.push({cell_type, execution_count:null, metadata:{}, outputs:[], source:source.split('\\n').map((x,i,a)=>x+(i<a.length-1?'\\n':''))});
add('markdown', '# Stage 3 - turnout preprocessing and EDA\\n\\nThis stage uses the certified municipality turnout table from Stage 2. It creates a primary ballot view, historical lag features, VAP-based registration rates where available, and denominator-aware descriptive summaries. It does not fit a predictive model yet.');
add('code', `turnout_path = PROJECT_ROOT / 'derived_municipality_turnout.csv'\\nif not turnout_path.exists():\\n    raise FileNotFoundError('Run the Stage 2 turnout aggregation cell first.')\\n\\nturnout_model = pd.read_csv(turnout_path)\\nfor col in ['election_year','registered_voters','ballot_votes_cast','spoilt_votes','turnout_pct','spoilt_rate_pct','reporting_units']:\\n    if col in turnout_model.columns:\\n        turnout_model[col] = pd.to_numeric(turnout_model[col], errors='coerce')\\n\\nturnout_model['municipality_code'] = turnout_model['municipality_code'].astype('string')\\nturnout_model['ballot_type'] = turnout_model['ballot_type'].astype('string').str.strip()\\nturnout_model['province'] = turnout_model['province'].astype('string').str.strip()\\n\\nballot_coverage = (\\n    turnout_model.groupby('ballot_type', dropna=False)\\n    .agg(rows=('ballot_type','size'), municipalities=('municipality_code','nunique'), years=('election_year','nunique'))\\n    .reset_index()\\n    .sort_values('rows', ascending=False)\\n)\\ndisplay(ballot_coverage)`);
add('code', `# Keep ballot types explicit. PR is the default primary view when available; review this choice before modelling.\\nPRIMARY_BALLOT_TYPE = 'PR' if 'PR' in turnout_model['ballot_type'].dropna().unique() else turnout_model['ballot_type'].dropna().iloc[0]\\nturnout_primary = turnout_model.loc[turnout_model['ballot_type'].eq(PRIMARY_BALLOT_TYPE)].copy()\\nturnout_primary = turnout_primary.sort_values(['municipality_code','election_year'])\\n\\nassert turnout_primary[['municipality_code','election_year']].duplicated().sum() == 0\\nassert turnout_primary['turnout_pct'].between(0,100,inclusive='both').all()\\n\\nturnout_primary['turnout_lag'] = turnout_primary.groupby('municipality_code')['turnout_pct'].shift(1)\\nturnout_primary['turnout_change_pp'] = turnout_primary['turnout_pct'] - turnout_primary['turnout_lag']\\nturnout_primary['registration_lag'] = turnout_primary.groupby('municipality_code')['registered_voters'].shift(1)\\nturnout_primary['registration_change_pct'] = 100 * (turnout_primary['registered_voters'] - turnout_primary['registration_lag']) / turnout_primary['registration_lag'].replace({0: pd.NA})\\n\\nprint('Primary ballot:', PRIMARY_BALLOT_TYPE)\\nprint('Primary turnout table:', turnout_primary.shape)\\ndisplay(turnout_primary.head())`);
add('code', `# Historical VAP and registration-rate context. The supplied VAP file currently covers 2000-2016 only.\\nvap_path = DATASETS_DIR / 'eligible_population_VAP_by_municipality_national.csv'\\nvap = pd.read_csv(vap_path, skiprows=3)\\nvap['municipality_code'] = vap['municipality_code'].astype('string').str.strip()\\nvap['election_year'] = pd.to_numeric(vap['election_year'], errors='coerce')\\nvap['voting_age_population'] = pd.to_numeric(vap['voting_age_population'], errors='coerce')\\nvap['registered_voters_vap'] = pd.to_numeric(vap['registered_voters'], errors='coerce')\\nvap['registration_rate_pct'] = 100 * vap['registered_voters_vap'] / vap['voting_age_population'].replace({0: pd.NA})\\n\\nvap_joinable = vap.dropna(subset=['municipality_code','election_year']).copy()\\nturnout_primary = turnout_primary.merge(\\n    vap_joinable[['municipality_code','election_year','voting_age_population','registration_rate_pct']],\\n    on=['municipality_code','election_year'], how='left'\\n)\\nprint('VAP join coverage:', f\\"{turnout_primary['voting_age_population'].notna().mean():.1%}\\")\\ndisplay(turnout_primary[['election_year','registration_rate_pct']].describe())`);
add('code', `# Denominator-aware national and provincial EDA summaries.\\ndef weighted_summary(frame, group_cols):\\n    out = (frame.groupby(group_cols, dropna=False)\\n           .agg(registered_voters=('registered_voters','sum'),\\n                ballots_cast=('ballot_votes_cast','sum'),\\n                municipalities=('municipality_code','nunique'),\\n                reporting_units=('reporting_units','sum'))\\n           .reset_index())\\n    out['weighted_turnout_pct'] = 100 * out['ballots_cast'] / out['registered_voters'].replace({0: pd.NA})\\n    return out\\n\\nnational_turnout_trend = weighted_summary(turnout_primary, ['election_year'])\\nprovince_turnout_trend = weighted_summary(turnout_primary, ['province','election_year'])\\n\\nlatest_year = int(turnout_primary['election_year'].max())\\nlatest_turnout = turnout_primary.loc[turnout_primary['election_year'].eq(latest_year)].copy()\\nlatest_turnout = latest_turnout.sort_values('turnout_pct')\\n\\nprint('Latest year:', latest_year)\\ndisplay(national_turnout_trend)\\ndisplay(province_turnout_trend.head(20))\\ndisplay(latest_turnout[['province','municipality_code','municipality_source','turnout_pct','registered_voters']].head(10))`);
add('code', `# Quality and coverage checks for EDA handoff.\\neda_quality = pd.DataFrame({\\n    'check': [\\n        'duplicate municipality-year keys',\\n        'turnout outside 0-100',\\n        'votes exceed registered',\\n        'missing municipality code',\\n        'missing turnout',\\n        'missing lagged turnout',\\n        'missing VAP rate',\\n    ],\\n    'count': [\\n        int(turnout_primary[['municipality_code','election_year']].duplicated().sum()),\\n        int((~turnout_primary['turnout_pct'].between(0,100,inclusive='both')).sum()),\\n        int((turnout_primary['ballot_votes_cast'] > turnout_primary['registered_voters']).sum()),\\n        int(turnout_primary['municipality_code'].isna().sum()),\\n        int(turnout_primary['turnout_pct'].isna().sum()),\\n        int(turnout_primary['turnout_lag'].isna().sum()),\\n        int(turnout_primary['registration_rate_pct'].isna().sum()),\\n    ]\\n})\\ndisplay(eda_quality)\\n\\n# Outputs for the next modelling and dashboard stages.\\nnational_turnout_trend.to_csv(PROJECT_ROOT / 'eda_national_turnout_trend.csv', index=False)\\nprovince_turnout_trend.to_csv(PROJECT_ROOT / 'eda_province_turnout_trend.csv', index=False)\\nlatest_turnout.to_csv(PROJECT_ROOT / 'eda_latest_municipality_turnout.csv', index=False)\\neda_quality.to_csv(PROJECT_ROOT / 'eda_turnout_quality_checks.csv', index=False)\\nprint('Stage 3 complete: turnout preprocessing and EDA summaries exported.')`);
fs.writeFileSync(path, JSON.stringify(notebook, null, 1));
console.log('Appended Stage 3 turnout preprocessing and EDA cells. Total cells:', notebook.cells.length);

