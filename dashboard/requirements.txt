# Dashboard only (also what Streamlit Community Cloud installs). It reads derived/, so it
# does not need scikit-learn, the Census files or the notebook to run.
streamlit==1.64.0
pandas==3.0.6
numpy==2.5.3
altair==6.3.0
pydeck==0.9.3
